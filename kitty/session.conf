# 2x2 grid layout
layout splits

# Top-left: btop
launch --location=hsplit btop

# Top-right: fastfetch with Umbreon
launch --location=vsplit sh -c "clear && fastfetch && zsh -i"

# Bottom-left: cmatrix (green)
launch --location=hsplit cmatrix -C green

# Bottom-right: cava
launch --location=vsplit cava
